apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-metrics
  namespace: data
  labels:
    app: backend
    tenant: data
spec:
  selector:
    matchLabels:
      app: backend
      tenant: data
  endpoints:
  - port: metrics
    path: /metrics
    interval: 30s

